import { SignupForm } from "@/components/auth/SignupForm"

export function SignupPage() {
  return (
    <section aria-label="Signup section" className="flex items-center justify-center min-h-[50vh]">
      <SignupForm />
    </section>
  )
} 