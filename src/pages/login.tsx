import { LoginForm } from "@/components/auth/LoginForm"

export function LoginPage() {
  return (
    <section aria-label="Login section" className="flex items-center justify-center min-h-[50vh]">
      <LoginForm />
    </section>
  )
} 